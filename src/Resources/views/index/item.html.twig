{% for field in entityMetadata.getFields() %}
    {% if entityMetadata.isEmbedded(field) %}
        {{ "embedded field to handle" }}
    {% else %}
        {% include '@Karross/index/field.html.twig' %}
    {% endif %}
{% endfor %}
