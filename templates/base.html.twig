<!DOCTYPE html>
<html {% block html_attributes %}lang="{{ html_locale() }}" dir="{{ text_direction() }}"{% endblock %}>

{# following needs to be discussed for now #}
  <head>
  {% block head %}
      {% block head_title %}
      <title>Karross</title>
      {% endblock %}
      {% block head_meta %}
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      {% endblock %}
  {% endblock %}
  </head>
  <body>
  <header class="k-header">
      <a class="k-header__brand" href="{{ path('karross_home') }}">
          <span>{% trans from 'Karross' %}k_header.title{% endtrans %}</span>
      </a>
  </header>
  {% block body %}
  {% endblock %}
  </body>
</html>
