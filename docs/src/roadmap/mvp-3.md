# MVP 3 — Filters & search

## Per-property filters <span class="k-status k-status--red">Proposed</span>

<table class="k-ticket k-ticket--red">
  <tbody>
    <tr><td><strong>Existing.</strong> None — list rendering only.</td></tr>
    <tr><td><strong>Expected.</strong> Filterable fields per entity; state persisted in the URL(?).</td></tr>
    <tr><td><strong>Prerequisites.</strong> MVP 1 — CRUD lane (forms pipeline).</td></tr>
  </tbody>
</table>

## Free-text search <span class="k-status k-status--red">Proposed</span>

<table class="k-ticket k-ticket--red">
  <tbody>
    <tr><td><strong>Existing.</strong> None.</td></tr>
    <tr><td><strong>Expected.</strong> Search across entity lists — full-text(?) and scope to decide.</td></tr>
    <tr><td><strong>Prerequisites.</strong> This MVP's Per-property filters; only after CRUD/forms are stabilized.</td></tr>
  </tbody>
</table>

## Reference documentation — update <span class="k-status k-status--red">Proposed</span>

<table class="k-ticket k-ticket--red">
  <tbody>
    <tr><td><strong>Existing.</strong> The reference page (created in MVP 1, updated in MVP 2) does not cover the filter/search surface.</td></tr>
    <tr><td><strong>Expected.</strong> Extend the reference with the filters and search configuration.</td></tr>
    <tr><td><strong>Prerequisites.</strong> This MVP's Per-property filters + Free-text search.</td></tr>
  </tbody>
</table>